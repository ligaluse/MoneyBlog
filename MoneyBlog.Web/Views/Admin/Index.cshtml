@model IEnumerable<MoneyBlog.DataLayer.Models.AspNetUser>
@using MoneyBlog.DataLayer.Models


@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<ul>
    <li>@Html.ActionLink("Create User Role", "CreateUserRole", "Admin")</li>
    <li>@Html.ActionLink("Assign User Role", "AssignUserRole", "Admin")</li>
</ul>
<p>All registered users:</p>
@Html.ActionLink("All users&details", "UsersWithRoles", "Admin")
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>

        <th></th>
    </tr>

    @foreach (var user in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelAspNetUser => user.Email)
            </td>
            <td>
                @Html.ActionLink("Edit", "EditUser", new { id = user.Id }) |
                @Html.ActionLink("Details", "UserDetails", new { id = user.Id }) |
                @Html.ActionLink("Delete", "DeleteUser", new { id = user.Id })
            </td>
        </tr>
    }

</table>*@
